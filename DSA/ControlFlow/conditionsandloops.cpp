#include <iostream>
using namespace std;

int main()
{
    /*
    int a,b;
    cin>>a>>b;
    // cout<< "Value of n is " << n <<endl;
    cout<< a << " " << b << endl;
    if(a>b)
    {
        cout<< "A is greater" << endl;
    }
    else{
        cout<< "B is greater" << endl;
    }

    // int c = cin.get(); // takes character and prints ASCII value
    // cout << c <<endl;
    */

    /*   char ch;
        cin >> ch;

        if (ch >= 'a' && ch <= 'z')
        {
            cout << "This is Lowercase" << endl;
        }
        else if (ch >= 'A' && ch <= 'Z')
        {
            cout << "This is Uppercase" << endl;
        }
        else if (ch >= '0' && ch <= '9')
        {
            cout << "This is Numeric" << endl;
        }
    */

    /*
    int n;
    cin >> n;
    int sum = 0;
    int i = 1;
    while (i <= n)
    {
        sum += i;
        i++;
    }
    cout << sum << endl;
    */

    // HomeWork

    // Sum of all even numbers from 1 to N

    /*
    int n;
    cin >> n;
    int sum = 0;
    int i = 1;
    while (i <= n)
    {
        if (i % 2 == 0)
        {
            sum += i;
        }
        i++;
    }
    cout << sum << endl;
    */

    // Fahrenheit to Celsius

    /*
    float f;
    cin >> f;
    float c = ((f - 32) * 5) / 9;
    cout << c <<endl;
    */

    // Prime Number is tha which is divisible by 1 and itself

    /*
    int n;
    cin >> n;
    bool c = 1;
    for(int i=2;i<n;i++)
    {
        if(n%i == 0)
        {
            cout<<"Number is not prime";
            c=0;
            break;
        }
    }
    if(c!=0)
    {
        cout<<"Number is prime" <<endl;
    }
    */


}