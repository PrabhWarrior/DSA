#include <iostream>
using namespace std;

int main()
{
    // int arr[10] = {2, 5, 6, 7};

    // cout << "Address of first memory block is " << arr << endl;
    // cout << arr[0] << endl;
    // cout << "Address of first memory block is " << &arr[0] << endl;

    // cout << "4th " << *arr << endl;
    // cout << "5th " << *arr + 1 << endl;
    // cout << "6th " << *(arr) + 1 << endl;
    // cout << "7th " << *(arr + 1) << endl;
    // cout << "8th " << arr[2] << endl;
    // cout << "9th " << *(arr + 2) << endl;

    // int i = 3;
    // cout << i[arr] << endl;

    // int temp[10] = {1, 2};
    // cout << "1st" << sizeof(temp) << endl;
    // cout << "2nd" << sizeof(*temp) << endl;
    // cout << "3rd" << sizeof(&temp) << endl;

    // int *ptr = &temp[0];
    // cout << sizeof(ptr) << endl;  // 8
    // cout << sizeof(*ptr) << endl; // 4
    // cout << sizeof(&ptr) << endl; // 8

    // int a[20] = {1,2,3,4,5};

    // cout << &a[0] << endl;
    // cout << &a << endl;
    // cout << a << endl << endl;

    // int *p = &a[0];
    // cout << p << endl; // pointer storing the address of array(type int) 
    // cout << *p << endl; // dereferencing the pointer
    // cout << &p << endl; // pointer's address


    // int arr[10];
    
    // // arr = arr + 1;   // Error
    // // incompatible types in assignment of 'int*' to 'int [10]

    // int *ptr = &arr[0];

    // cout << ptr << endl;
    // ptr = ptr + 1;
    // cout << ptr << endl;

    return 0;
}